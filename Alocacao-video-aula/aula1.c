#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Problema simples: quando temos um programa que não sabemos o tamanho, definimos ele com um tamanho grande
    int vetor[1000000];


    return 0;

    // Considere o seguinte:
    //  - Arrays são agrupamentos sequenciais de dados de um mesmo tipo na memória.
    //  - um ponteiro guarda o endereço de um dado na memória
    //  - O nome do array é um ponteiro para o primeiro elemento do array


    /* 
        4 funções:
            - malloc: aloca um espaço de memória
            - calloc: aloca um espaço de memória e inicializa com 0
            - realloc: realoca um espaço de memória
            - free: libera um espaço de memória
    */

   /* 
        Operador:
            - sizeof: retorna o tamanho em bytes de um tipo de dado
   */
}

